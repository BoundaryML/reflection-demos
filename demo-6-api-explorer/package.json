{
  "name": "demo-6-api-explorer",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently -k -n backend,frontend \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:mock": "concurrently -k -n backend,frontend \"npm --prefix backend run dev:mock\" \"npm --prefix frontend run dev\"",
    "baml:generate": "../scripts/baml-dev generate"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
